name: Import Studigo Mascot Frame

on:
  push:
    branches:
      - assets/cursor-tracking-mascot-frames
    paths:
      - '.github/workflows/import-mascot-frame.yml'

permissions:
  contents: write

jobs:
  import-frame:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: assets/cursor-tracking-mascot-frames

      - name: Download frame
        run: |
          mkdir -p apps/web/public/mascot/cursor-tracking
          curl -fL 'https://d2ol7oe51mr4n9.cloudfront.net/user_3IkKBBXHZvEZQt2d8pBaRYd083T/2c5f750a-52c4-44cb-a3ae-ca57ed460486.png' -o 'apps/web/public/mascot/cursor-tracking/studigo-cursor-frame-09-down-left.png'
          file 'apps/web/public/mascot/cursor-tracking/studigo-cursor-frame-09-down-left.png'

      - name: Commit imported frame
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add 'apps/web/public/mascot/cursor-tracking/studigo-cursor-frame-09-down-left.png'
          git diff --cached --quiet && exit 0
          git commit -m 'assets: add mascot cursor frame 09 down-left'
          git push origin HEAD:assets/cursor-tracking-mascot-frames
